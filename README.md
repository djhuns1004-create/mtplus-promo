# 먹튀플러스 V6 FIXED

최신 콘텐츠 카드가 한 줄 텍스트로 붙는 현상을 방지하기 위해:
- assets/style.css?v=6.1 캐시 버스터 적용
- 최신 콘텐츠 핵심 CSS를 index.html에도 안전하게 인라인 적용

GitHub 저장소 루트에 전체 파일을 덮어써 주세요.
특히 `index.html`과 `assets/style.css` 두 파일은 반드시 함께 교체해야 합니다.
